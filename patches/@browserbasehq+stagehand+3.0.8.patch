diff --git a/node_modules/@browserbasehq/stagehand/dist/index.js b/node_modules/@browserbasehq/stagehand/dist/index.js
index a0120e7..c1debf9 100644
--- a/node_modules/@browserbasehq/stagehand/dist/index.js
+++ b/node_modules/@browserbasehq/stagehand/dist/index.js
@@ -25660,7 +25660,15 @@ Do not include any other text, formatting or markdown in your output. Do not inc
             }
             let parsedData;
             try {
-              parsedData = JSON.parse(extractedData);
+              try {
+                parsedData = JSON.parse(extractedData);
+              } catch (parseErr) {
+                if (parseErr instanceof SyntaxError && typeof extractedData === "string") {
+                  parsedData = JSON.parse(require("jsonrepair").jsonrepair(extractedData));
+                } else {
+                  throw parseErr;
+                }
+              }
               validateZodSchema(options.response_model.schema, parsedData);
             } catch (e2) {
               const isParseError = e2 instanceof SyntaxError;
@@ -37781,10 +37789,7 @@ var AgentProvider = class _AgentProvider {
     if (normalized in modelToAgentProviderMap) {
       return modelToAgentProviderMap[normalized];
     }
-    throw new UnsupportedModelError(
-      Object.keys(modelToAgentProviderMap),
-      "Computer Use Agent"
-    );
+    return "openai";
   }
 };
 
@@ -41124,7 +41129,16 @@ ${parsedSchema}
       });
       if (options.response_model) {
         const extractedData = response.choices[0].message.content;
-        const parsedData = JSON.parse(extractedData);
+        let parsedData;
+        try {
+          parsedData = JSON.parse(extractedData);
+        } catch (parseErr) {
+          if (parseErr instanceof SyntaxError && typeof extractedData === "string") {
+            parsedData = JSON.parse(require("jsonrepair").jsonrepair(extractedData));
+          } else {
+            throw parseErr;
+          }
+        }
         try {
           validateZodSchema(options.response_model.schema, parsedData);
         } catch (e2) {
@@ -66105,10 +66119,7 @@ var LLMProvider = class {
         logger: this.logger
       });
     }
-    const provider = modelToProviderMap[modelName];
-    if (!provider) {
-      throw new UnsupportedModelError(Object.keys(modelToProviderMap));
-    }
+    const provider = modelToProviderMap[modelName] || "openai";
     const availableModel = modelName;
     switch (provider) {
       case "openai":
